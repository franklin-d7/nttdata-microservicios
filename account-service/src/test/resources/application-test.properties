spring.application.name=account-test

# R2DBC PostgreSQL Configuration for E2E Tests
spring.r2dbc.url=r2dbc:postgresql://localhost:5432/account_db
spring.r2dbc.username=nttdata
spring.r2dbc.password=nttdata123

# Flyway Configuration for E2E Tests
spring.flyway.enabled=true
spring.flyway.url=jdbc:postgresql://localhost:5432/account_db
spring.flyway.user=nttdata
spring.flyway.password=nttdata123
spring.flyway.baseline-on-migrate=true
spring.flyway.validate-on-migrate=false
spring.flyway.locations=classpath:db/migration

# Disable Kafka for tests
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration

# Logging
logging.level.root=INFO
logging.level.com.nttdata.account=DEBUG
